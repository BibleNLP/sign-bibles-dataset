resources:
  cloud: lambda
  accelerators: GH200:1   # 1x T4 GPU
  use_spot: true       # cheaper (preemptible), can stop anytime

run: |
  echo "Running on CPU only"
  python -c "print(sum(range(1000000)))"
  echo "Done"
